set -e
pushd $(dirname "$0")/..
git diff *.nix
git add .
echo "NixOS Rebuilding..."
nix run
gen=$(nixos-rebuild list-generations | grep current)
hostname=$(hostname)
git commit -am "$hostname $gen"
popd